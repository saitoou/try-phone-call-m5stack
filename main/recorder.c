#include "recorder.h"

#include "esp_log.h"

static const char *TAG = "recorder";

esp_err_t recorder_init(void)
{
    ESP_LOGI(TAG, "Phase 1 stub: recorder initialization is deferred");
    return ESP_OK;
}
